# gui package — shared GUI base classes, tab widgets, and main app
#
# Submodules:
#   base_tab.py — RedirectText + BaseFarmTab (tkinter scaffolding for all farm tabs)
#   tabs.py     — 5 concrete tab classes + 2 reusable mixins
#   app.py      — FarmGUI main window (uses core.registry for tab system)
#
# Entry point:
#   python -m gui.app
